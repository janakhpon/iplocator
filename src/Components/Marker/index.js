import React from 'react';
import styles from './index.css';


const Marker = () => {

    return(
        <div className={styles.mapMarker}>h
        <i className={`fa fa-map-marker fa-lg`} />
      </div>
    );
}

export default Marker;